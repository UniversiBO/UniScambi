<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" sizes="16x16" href="{{ asset('favicon.ico') }}" />

    <title>{% block title 'IngUnibo' %}</title>

    <!-- Custom styles for this template -->
    <style type="text/css">
        .sorted {
          text-decoration: underline;
        }
        body {
            padding-top: 70px;
        }        
    </style>

    <!-- Bootstrap core CSS -->
    <link rel="stylesheet" href="{{ asset('bootstrap/css/bootstrap.css') }}" />
    <link rel="stylesheet" href="{{ asset('bootstrap-editable/css/bootstrap-editable.css') }}" />
    <link rel="stylesheet" href="{{ asset('bootstrap-datetimepicker/css/bootstrap-datetimepicker.min.css') }}" />

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->

    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="{{ asset('bootstrap/js/bootstrap.js') }}"></script>   
    <script src="{{ asset('bootstrap-editable/js/bootstrap-editable.js') }}"></script>    
    <script src="{{ asset('bootstrap-datetimepicker/js/moment.js') }}"></script>
    <script src="{{ asset('bootstrap-datetimepicker/js/bootstrap-datetimepicker.js') }}"></script>
  </head>

  <body> 

    <!-- Fixed navbar -->
    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ path('ing_unibo_scambi_homepage') }}">UniScambi</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li><a href="{{ path('ing_unibo_scambi_list_corsi') }}">Corsi</a></li>
            <!--<li class="dropdown">
              <a a href="#" class="dropdown-toggle" data-toggle="dropdown">Item <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><a href="#">Elem</a></li>
                <li><a href="#">Elem</a></li>
              </ul>
            </li>-->
            <li><a href="{{ path('ing_unibo_scambi_list_dipartimenti') }}">Dipartimenti</a></li>            
            <li><a href="{{ path('ing_unibo_scambi_list_sedi') }}">Sedi</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <!-- render(controller('XXX:Navbar:user')) -->
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>

    <div class="container">
    {% for flashMessage in app.session.flashbag.get('success') %}
    <div class="alert alert-success alert-dismissable">{{ flashMessage }}</div>
    {% endfor %}

    {% for flashMessage in app.session.flashbag.get('warning') %}
    <div class="alert alert-warning alert-dismissable">{{ flashMessage }}</div>
    {% endfor %} 

        {% block content %}{% endblock %}
    </div> <!-- /container -->



  </body>
</html>
